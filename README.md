# VITE PROJECT

## Use JSON to Populate an HTML Page
- Create a dedicated folder for this portion if necessary.

- Add this data to your script.js

- Using JSON.stringify(), convert userData into a JSON string and log it to the console.

- Parse the JSON string back into a JavaScript object using `JSON.parse()`

- Using JavaScript and HTML, populate the HTML with the following data:
    - Full Name (Header)
    - Age (Subheader)
    - Email (Address - specify it’s an email)
    - Phone (Address - specify it’s a telephone number)
- List all the favorite meals in a list using an unordered list
